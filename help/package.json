{
  "name": "help",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p build && echo -n '<!doctype html>' > build/help.html && cat help.md | marked -o build/help.html",
    "watch": "chokidar *.md --initial -c 'yarn build'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "markdown": "^0.5.0",
    "markdown-to-html": "^0.0.13",
    "marked": "^0.7.0"
  },
  "devDependencies": {
    "chokidar": "^3.2.2"
  }
}
